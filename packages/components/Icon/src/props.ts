/**
 * 加载图标 Props 接口
 */
export interface LoadingIconProps {
  /**
   * 图标大小
   * @default '1em'
   */
  size?: string | number
  /**
   * 图标颜色
   * @default 'currentColor'
   */
  color?: string
  /**
   * 线条宽度
   * @default 2
   */
  strokeWidth?: number
}
